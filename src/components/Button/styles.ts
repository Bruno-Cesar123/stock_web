import styled from "styled-components";

export const Container = styled.input`
    margin-top: 16px;
    width: 400px;
    height: 50px;
    border-radius: 32px;
    cursor: pointer;
    background: #7652B2;
    border: none;
    
    font-size: 24px;
    color: #FFFFFF;

    &:hover {
        filter: brightness(0.9);
    }
`